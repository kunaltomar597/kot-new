# apps/control-plane: Vendor Control Plane (C11)

The vendor's cloud application: tenants, activation codes, licences and subscriptions, fleet health,
release channels and rollouts, support diagnostics, AI menu import. NestJS API + React web, managed
PostgreSQL in an India region, separate dev/staging/prod.

Built by: P0-17 (heartbeat + update channel), P6-04, P7-01 to P7-04, P7-08. Not started yet.
