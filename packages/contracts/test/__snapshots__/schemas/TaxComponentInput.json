{
  "input": {
    "type": "object",
    "properties": {
      "code": { "type": "string", "pattern": "^[A-Z][A-Z0-9]{0,9}$" },
      "rateBp": { "type": "integer", "minimum": 0, "maximum": 10000 }
    },
    "required": ["code", "rateBp"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "code": { "type": "string", "pattern": "^[A-Z][A-Z0-9]{0,9}$" },
      "rateBp": { "type": "integer", "minimum": 0, "maximum": 10000 }
    },
    "required": ["code", "rateBp"],
    "additionalProperties": false
  }
}
