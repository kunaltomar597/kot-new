{
  "input": {
    "type": "string",
    "enum": ["ACTIVE", "ESCALATED", "ACKNOWLEDGED", "CANCELLED", "RESOLVED"]
  },
  "output": {
    "type": "string",
    "enum": ["ACTIVE", "ESCALATED", "ACKNOWLEDGED", "CANCELLED", "RESOLVED"]
  }
}
