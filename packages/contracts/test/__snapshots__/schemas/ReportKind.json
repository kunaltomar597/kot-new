{
  "input": {
    "type": "string",
    "enum": ["SALES", "ITEMS", "PAYMENTS", "SHIFTS", "GST", "INVOICE_REGISTER"]
  },
  "output": {
    "type": "string",
    "enum": ["SALES", "ITEMS", "PAYMENTS", "SHIFTS", "GST", "INVOICE_REGISTER"]
  }
}
