# @rp/api-client

Typed REST and Socket.io client used by the console, desktop, waiter app, table tablet and QR site.
Handles device and staff tokens, refresh, idempotency keys, `ApiError` mapping and resync after
reconnect. Built by: P0-14. Not started yet.
